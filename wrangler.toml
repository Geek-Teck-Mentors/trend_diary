name = "trend-diary"
compatibility_date = "2025-04-01"
compatibility_flags = [ "nodejs_compat" ]
main = "./dist/server/index.js"
preview_urls = true

# ES Module形式を明示的に指定
[build]
format = "modules"

[assets]
directory = "./dist/client/"
binding = "ASSETS"
run_worker_first = true # ミドルウェアを挟むため
