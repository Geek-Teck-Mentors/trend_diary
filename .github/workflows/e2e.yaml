name: E2E

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - '.github/workflows/e2e.yaml'
      - 'src/test/e2e/**'
      - 'src/application/web/routes/**'
      - 'package.json'
      - 'package-lock.json'

# カバレッジの可視化で、コメントを残すための権限
permissions:
  contents: read
  pull-requests: write

# 連続push時に同じジョブが並列で実行されるのを防ぐ
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# 共通の設定を定義
env:
  NODE_VERSION_FILE: .node-version

jobs:
  test:
    name: Main
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - id: setup-supabase
        name: Supabaseのセットアップ
        uses: ./.github/actions/setup_supabase

      - name: 環境変数の設定
        run: |
          echo "SUPABASE_ANON_KEY=${{ steps.setup-supabase.outputs.anon_key }}" >> $GITHUB_ENV
          echo "DATABASE_URL=${{ steps.setup-supabase.outputs.db_url }}" >> $GITHUB_ENV

      - name: E2E環境のセットアップ
        uses: ./.github/actions/setup_e2e
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}
          database-url: ${{ steps.setup-supabase.outputs.db_url }}

      - name: E2Eテストの実行
        run: npm run e2e -- --pass-with-no-tests

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  test-diff:
    name: Diff
    if: github.event_name == 'pull_request'
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: setup-supabase
        name: Supabaseのセットアップ
        uses: ./.github/actions/setup_supabase

      - name: 環境変数の設定
        run: |
          echo "SUPABASE_ANON_KEY=${{ steps.setup-supabase.outputs.anon_key }}" >> $GITHUB_ENV
          echo "DATABASE_URL=${{ steps.setup-supabase.outputs.db_url }}" >> $GITHUB_ENV

      - name: E2E環境のセットアップ
        uses: ./.github/actions/setup_e2e
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}
          database-url: ${{ steps.setup-supabase.outputs.db_url }}

      - name: E2Eテストの差分実行
        shell: bash
        run: |
          set -euo pipefail
          npm run e2e -- --reporter=github --only-changed=origin/$GITHUB_BASE_REF
