name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# 連続push時に同じジョブが並列で実行されるのを防ぐ
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# 共通の設定を定義
env:
  NODE_VERSION_FILE: .node-version

jobs:
  config:
    runs-on: ubuntu-latest
    outputs:
      runner: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Node.jsの構築
        uses: ./.github/actions/setup_node
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}
      - run: echo "Config completed"

  lint:
    needs: config
    runs-on: ${{ needs.config.outputs.runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Node.jsの構築
        uses: ./.github/actions/setup_node
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}

      - name: Lint
        run: npm run lint

  format:
    needs: config
    runs-on: ${{ needs.config.outputs.runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Node.jsの構築
        uses: ./.github/actions/setup_node
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}

      - name: Format
        run: npm run format

  # 本来であれば、概念的にコンパイルはLint/Formatと同列ではないが、実行時間短縮のために並列実行にしている
  ts-compile-check:
    needs: config
    runs-on: ${{ needs.config.outputs.runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Node.jsの構築
        uses: ./.github/actions/setup_node
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}

      - name: TypeScript型チェック
        run: npm run check-types

  test-config:
    needs: [lint, format, ts-compile-check]
    runs-on: ubuntu-latest
    outputs:
      test_runner: ubuntu-latest
    steps:
      - name: Node.jsの構築
        uses: ./.github/actions/setup_node
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}
      - run: echo "Test configuration completed"

  test-frontend:
    needs: test-config
    runs-on: ${{ needs.test-config.outputs.test_runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Node.jsの構築
        uses: ./.github/actions/setup_node
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}

      - name: フロントエンドのテスト
        uses: ./.github/actions/test_cov
        with:
          target: frontend

  test-backend:
    needs: test-config
    runs-on: ${{ needs.test-config.outputs.test_runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Node.jsの構築
        uses: ./.github/actions/setup_node
        with:
          node-version-file: ${{ env.NODE_VERSION_FILE }}

      - name: バックエンドのテスト
        uses: ./.github/actions/test_cov
        with:
          target: backend
