name: Functions CI

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/**'
      - '.github/workflows/functions_ci.yaml'
  pull_request:
    branches:
      - main
    paths:
      - 'supabase/**'
      - '.github/workflows/functions_ci.yaml'

env:
  DENO_VERSION: 2.3.1
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Denoの構築
        uses: denoland/setup-deno@v2
        with:
          deno-version: ${{ env.DENO_VERSION }}
      - name: Lint
        run: deno lint -c supabase/style_config.json

  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Denoの構築
        uses: denoland/setup-deno@v2
        with:
          deno-version: ${{ env.DENO_VERSION }}
      - name: Format
        run: deno fmt --check -c supabase/style_config.json

  type-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Denoの構築
        uses: denoland/setup-deno@v2
        with:
          deno-version: ${{ env.DENO_VERSION }}
      - name: Type Check
        # deno checkのファイル指定で`**`を使うと、サブディレクトリのファイルがチェックされなくなるため、全部のディレクトリを手動でそれぞれ指定する
        # deno checkはdeno.jsonのある環境でしか実行できないため、各関数のdeno.jsonを指定する
        run: |
          for function_name in $(ls supabase/functions); do
            find supabase -name "*.ts" | xargs deno check -c supabase/functions/$function_name/deno.json
          done

  test:
    needs: [lint, format, type-check]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Denoの構築
        uses: denoland/setup-deno@v2
        with:
          deno-version: ${{ env.DENO_VERSION }}
      - name: setup supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: supabase start
        run: supabase start
      - name: setup env
        run: |
          echo "SUPABASE_URL=$(supabase status --output json | jq -r '.API_URL')" >> $GITHUB_ENV
          echo "SUPABASE_ANON_KEY=$(supabase status --output json | jq -r '.ANON_KEY')" >> $GITHUB_ENV
      - name: exec tests
        # deno testはdeno.jsonのある環境でしか実行できないため、各関数のディレクトリで実行する
        run: |
          echo "Running tests for Supabase Functions"
          echo $SUPABASE_URL
          echo $SUPABASE_ANON_KEY
          echo "RUNNING TESTS"
          # 共通のcoverageを格納するファイルを作成
          touch coverage.txt
          COVERAGE_DIR=$(pwd)/coverage.txt

          for function_name in $(ls supabase/functions); do
            # サブシェルで実行することで、cdの影響を局所化
            (
              cd supabase/functions/$function_name

              # deno testの実行
              deno test --allow-read --allow-env --allow-net --allow-write --allow-run -c deno.json --coverage=coverage


              # カバレッジの対象外ファイルを取得
              EXCLUDE_FILES=$(yq '.exclude[]' coverage_config.yml | xargs | sed 's/ /|/g')

              # カバレッジの結果を共通のファイルにコピー
              echo "Coverage for $function_name" >> "$COVERAGE_DIR"
              deno coverage ./coverage --exclude="$EXCLUDE_FILES" >> "$COVERAGE_DIR"
            )
          done
        env:
          SUPABASE_URL: ${{ env.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ env.SUPABASE_ANON_KEY }}
      - name: Output coverage
        # カバレッジの結果をGitHubのPRコメントに出力
        run: |
          # 共通のカバレッジファイルを出力
          gh pr comments ${{ github.event.pull_request.number }} -b "$(cat $COVERAGE_DIR)"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

